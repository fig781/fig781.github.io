source "https://rubygems.org"

# This will help ensure the Jekyll version on GitHub Pages matches your local build
gem "github-pages", group: :jekyll_plugins

# Additional plugins if needed
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
end
